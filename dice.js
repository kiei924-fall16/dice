// e.g. getRandomInt(1, 6)
var getRandomInt = function(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

$(function() {
  // Your code here!
  // Hint: .attr() is used to manipulate the value of an element's attribute
  // Hint: dice images can be found at http://golearntocode.com/images/dice/number.png
})